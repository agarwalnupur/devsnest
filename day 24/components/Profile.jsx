// import { Redirect } from "react-router";

const Profile = () => {
    // if(!loggedIn)
    // {
    //    return(<Redirect to= '/'/>) 
    // }
    return( <h1>Profile</h1>)
}

export default Profile;

// {loggedIn}